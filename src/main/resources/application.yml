spring:
  datasource:
    url: jdbc:postgresql://localhost:8880/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
  thymeleaf:
    enabled: true
    encoding: utf-8
    cache: false
    check-template: true
    check-template-location: true
